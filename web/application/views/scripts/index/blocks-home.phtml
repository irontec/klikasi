<div class="bloque-interior col-sm-4 text-center">
    <figure>
        <img src="multimedia/home-page/irudia/{{id}}" alt="{{title}}" />
    </figure>
    <header>
        <h4>{{title}}</h4>
        <p>{{description}}</p>
    </header>
</div>